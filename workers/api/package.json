{
  "name": "groundsnearme-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GroundsNearMe API — Cloudflare Worker in front of Supabase + R2",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "tail": "wrangler tail",
    "test": "node --test \"test/*.test.js\"",
    "typecheck": "echo \"plain JS — nothing to typecheck\""
  },
  "devDependencies": {
    "wrangler": "3.114.1"
  }
}
